The Prompt Splitter Agent parses a free-form user query about a commercial space in Bangalore and extracts structured fields (business type, area in square feet, and address/location). It then generates four tailored sub-prompts: one for the Electricity Summarizer Agent, one for the GIS Agent, one for the Legal Compliance Agent, and one for the Water Resource Agent, formatted so each sub-agent receives exactly the inputs it needs.